/**
 * Canonical site URL — single source of truth for sitemap, robots,
 * metadataBase and per-route canonicals.
 *
 * vantadb.vercel.app is the currently deployed host; if the vanity
 * domain is ever purchased/resolved, change it here (and re-point any
 * hardcoded per-route canonicals).
 */
export const SITE_URL = "https://vantadb.vercel.app";
